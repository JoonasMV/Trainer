<resources>
    <!-- Base application theme. -->
    <style name="Theme.Trainer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Top bar and borders -->
        <item name="colorPrimary">@color/menu_items_dark</item>
        <item name="android:statusBarColor">@color/menu_items_dark</item>
        <item name="backgroundColor">@color/background_dark</item>
        <item name="actionBarStyle">@style/action_bar_dark</item>-->

        <!-- Buttons -->
        <item name="colorPrimaryVariant">@color/menu_items_dark</item>

        <!-- Text color -->
        <item name="android:textColor">@color/text_color_dark</item>
        <item name="colorSecondary">@color/text_color_dark</item>
    </style>

    <style name="underline_color">
        <item name="android:background">@color/underline_color_dark</item>
    </style>

    <style name="action_bar_dark" parent="Widget.MaterialComponents.ActionBar.Primary">
        <item name="background">@color/menu_items_dark</item>
    </style>

    <style name="image_style">
        <item name="android:tint">@color/white</item>
    </style>

</resources>